# Branches

Source: private kit TheFLOW (`real-limitless/TheFLOW`). Do not fork these rules here.

| Branch | Audience | What is on it |
| --- | --- | --- |
| `CORE` (this branch) | Everyone | Concept, campaign, site, catalog schema. No gateway source. |
| `DEVELOPMENT` | Operators | Runnable gateway. Compose is the supported run path. |
| `main` | Operators | Product alias. |
| `catalog-data` | Pages / consumers | Published catalog shards. Not CORE. Not DEVELOPMENT. |

If you only see markdown and `site/`, you are on CORE.

```sh
git checkout DEVELOPMENT
```
