{
  "id": "io.github.seunghan91/ainote",
  "title": "ainote",
  "description": "Agent-native notes, tasks, dev-docs, vaults, sync & handoffs. MCP + OpenAPI dual surface.",
  "summary": "Agent-native notes, tasks, dev-docs, vaults, sync & handoffs. MCP + OpenAPI dual surface.",
  "offersHint": "Agent-native notes, tasks, dev-docs, vaults, sync & handoffs. MCP + OpenAPI dual surface. · Remote MCP (streamable-http) at https://api.ainote.dev/api/mcp · Packages: npm:@ainote/mcp · Requires headers: Authorization · Env vars: AINOTE_API_KEY",
  "version": "1.3.2",
  "status": "inactive",
  "transport": "streamable-http",
  "endpointUrl": "https://api.ainote.dev/api/mcp",
  "remotes": [
    {
      "type": "streamable-http",
      "url": "https://api.ainote.dev/api/mcp",
      "headers": [
        {
          "name": "Authorization",
          "description": "McpKey <YOUR_KEY> -- same key as the AINOTE_API_KEY env var for the stdio transport.",
          "secret": true,
          "required": true
        }
      ]
    }
  ],
  "install": {
    "kind": "npm",
    "package": "@ainote/mcp",
    "version": "1.3.2",
    "transport": "stdio",
    "environmentVariables": [
      {
        "name": "AINOTE_API_KEY",
        "description": "64-character ainote MCP key. Issue one via `npx @ainote/mcp signup` (RFC 8628 device flow), or in a Claude session by calling signup_and_get_key, or at https://app.ainote.dev -> Settings -> MCP keys.",
        "secret": true,
        "required": true
      }
    ]
  },
  "packages": [
    {
      "kind": "npm",
      "package": "@ainote/mcp",
      "version": "1.3.2",
      "transport": "stdio",
      "environmentVariables": [
        {
          "name": "AINOTE_API_KEY",
          "description": "64-character ainote MCP key. Issue one via `npx @ainote/mcp signup` (RFC 8628 device flow), or in a Claude session by calling signup_and_get_key, or at https://app.ainote.dev -> Settings -> MCP keys.",
          "secret": true,
          "required": true
        }
      ]
    }
  ],
  "environmentVariables": [
    {
      "name": "AINOTE_API_KEY",
      "description": "64-character ainote MCP key. Issue one via `npx @ainote/mcp signup` (RFC 8628 device flow), or in a Claude session by calling signup_and_get_key, or at https://app.ainote.dev -> Settings -> MCP keys.",
      "secret": true,
      "required": true
    }
  ],
  "flags": [
    "remote",
    "stdio",
    "repo-offline"
  ],
  "homepage": "https://docs.ainote.dev",
  "sourceUrl": "https://github.com/seunghan91/ainote",
  "repository": {
    "url": "https://github.com/seunghan91/ainote",
    "source": "github"
  },
  "updatedAt": "2026-05-14T11:01:40.815105Z",
  "publishedAt": "2026-05-14T11:01:40.815105Z",
  "provenance": "official-registry",
  "requiresHeaders": [
    "Authorization"
  ],
  "headerDocs": [
    {
      "name": "Authorization",
      "description": "McpKey <YOUR_KEY> -- same key as the AINOTE_API_KEY env var for the stdio transport.",
      "secret": true,
      "required": true
    }
  ],
  "enrichment": {
    "normalizedAt": "2026-08-12T01:01:43.551Z",
    "sourceRepoAt": "2026-08-11T23:21:23.456Z",
    "readmeAt": "2026-08-12T01:01:43.551Z",
    "toolsAt": "2026-08-11T23:21:23.897Z",
    "complete": true
  },
  "sourceRepo": {
    "status": "not_found",
    "url": "https://github.com/seunghan91/ainote",
    "checkedAt": "2026-08-11T23:21:23.456Z",
    "httpStatus": 404,
    "error": "repository HTTP 404",
    "host": "github"
  },
  "readme": {
    "source": "github",
    "fetchedAt": "2026-08-12T01:01:43.551Z",
    "error": "README.md not found on main/master"
  },
  "toolsPreview": [
    {
      "name": "delete_task",
      "description": "Soft-delete a task by ID. Destructive but reversible within 30 days (TaskCleanupJob purges trash daily at 2am KST). Returns 404 if the task does not exist or is not owned by the authenticated user."
    },
    {
      "name": "list_papers",
      "description": "List notes/papers from AI Note. Supports keyword search across title and content, category filtering, pagination, and sorting. Returns id/title/content_preview/category_id/created_at. Use this when the user asks 'find my note about X', 'what did I write on Y', or wants to search their knowledge base."
    },
    {
      "name": "project_read",
      "description": "List the authenticated user's projects (L2 intent layer), or fetch one by id. Each entry includes riskiest_assumption — the single highest-priority unverified assumption (or, if all are verified, the highest-criticality one)."
    },
    {
      "name": "project_write",
      "description": "Create or update a project (L2 intent layer). Omit `id` to create, pass it to update. There is no delete tool — use status: 'archived' to retire a project."
    },
    {
      "name": "assumption_write",
      "description": "Create or update an assumption under a project (L2 intent layer). Omit `id` to create, pass it to update. There is no delete tool.\n\n🔴 `verdict` is REJECTED with an error if passed — verdict is confirmed only through a human judgment decision (see judgment_submit). This tool can only shape the assumption itself: what is being assumed, how critical it is, and what evidence would settle it.\n"
    },
    {
      "name": "judgment_submit",
      "description": "Request a human judgment (L3 layer) by attaching evidence — approval, evidence, or verdict. This is the ONLY door an agent has into the judgment queue.\n\n🔴 `decision` is REJECTED with an error if passed — judgments are always created with decision='pending'. Only a human decides them (from the /command queue). Use judgment_list afterwards to check whether this judgment has since been decided.\n\nrisk_tier controls urgency: 1=destructive (blocks execution until decided), 2=reversible (may auto-proceed after a wait window), 3=informational (auto-expires after 24h).\n"
    },
    {
      "name": "judgment_list",
      "description": "List the authenticated user's own judgments (L3 layer) — use this to check your own blocking status (risk_tier 1 pending = execution blocked). Default order matches the /command queue: risk_tier ascending, then arrival order."
    },
    {
      "name": "delete_dev_doc",
      "description": "Soft-delete a dev document by title or UUID. Reversible from trash. Pass `category` when multiple docs share the same title across subcategories (memory/claude/cursor/env/docs)."
    },
    {
      "name": "get_setup_guide",
      "description": "Get instructions for setting up AI Note MCP in Claude Desktop, Cursor, or other MCP clients. No authentication required."
    },
    {
      "name": "vault_create",
      "description": "Create a new private vault as a GitHub repository under the user's account. Requires the user to have completed the GitHub App install flow first."
    },
    {
      "name": "vault_sync",
      "description": "Wrapper around vault file sync. action=list|pull|push to work against the primary vault. For push: WAF-bypass via `content_b64` or `content: '__B64__:...'` prefix (mirrors sync_push)."
    },
    {
      "name": "sync_push",
      "description": "Push a markdown file into the primary vault. Optional CAS via base_sha to detect concurrent multi-PC writes (Layer 3 of multi-PC sync plan). WAF-bypass tip: large bodies (~10KB+) that get false-positive blocked at Cloudflare can be sent via `content_b64` (base64-encoded) OR `content` prefixed with `__B64__:` instead. When the push CREATES a new .md file, the response may carry `dedup_candidates` — existing memory files that look similar (keyword + title similarity, no LLM). It is an advisory, not a rejection: YOU decide to merge into one of them (re-push that path), drop this write as a duplicate (skip), or keep it as a new memory (store)."
    },
    {
      "name": "sync_push_batch",
      "description": "Push MANY files into the primary vault in ONE call. Use this instead of looping sync_push whenever you have more than a couple of changed paths — the cost of sync_push is round trips, not bytes. Per-item semantics are identical to sync_push (Layer 3 base_sha CAS, superseded contract, content_b64 WAF fallback). Partial success is normal and returns HTTP 200: read `results[]` and match entries by `path`, NOT by position. A conflict result always carries `remote_sha` + `conflict_reason` so you can go straight to sync_merge. Request-level failures (duplicate paths in one batch, idempotency key reused with a different payload, >200 items, >4MB) return 4xx and apply NOTHING. Each item MUST carry `content_sha` (SHA1 hex of the decoded body, same algorithm as git_sha) — it powers both the integrity check and the retry-safe `skipped_identical` result. Newly created .md items may carry `dedup_candidates` (same merge/skip/store advisory as sync_push); a batch creating more than 10 new files skips the check entirely and reports `dedup_skipped: \"bulk\"`."
    },
    {
      "name": "sync_delete",
      "description": "Delete a file from the primary vault. Optional CAS via base_sha to detect concurrent multi-PC writes. Protected paths (global/memory/, global/skills/, global/planning/, global/claude-config/, handoffs/) require base_sha or explicit force:true (audited). Idempotent: deleting a non-existent path returns success with deleted:false."
    },
    {
      "name": "sync_audit_layer5",
      "description": "Record the result of a client-side Layer 5 codex review (sync.py merge gate) as a vault_events row. Body of the review is NOT stored — only an HMAC digest of the summary so operators can correlate without exposing review content. Opt-in: server skips writes (still returns success) unless ENV['AINOTE_LAYER5_AUDIT']='on' AND a versioned HMAC secret is configured. Used by the sync.py SessionStart hook + merge command to surface 'why was this blocked?' across multi-PC sessions."
    },
    {
      "name": "handoff_save",
      "description": "Save a session handoff note for cross-device / cross-session continuation. Stored at handoffs/{project}-{topic}-{YYYY-MM-DD}.txt in the user's primary vault. Use the optional `time` param (HHMM, KST) to disambiguate multiple handoffs saved on the same day — it is appended to the topic slug (e.g. topic='phase-d', time='1555' → handoffs/{project}-phase-d-1555-{date}.txt)."
    },
    {
      "name": "graph_soft_delete",
      "description": "Soft-delete an env_sync graph node (sets deleted_at; .live scope hides it from reads). Idempotent — re-deleting a deleted node is a no-op success."
    },
    {
      "name": "env_sync.secret_push",
      "description": "Push a new client-encrypted secret. Ciphertext only — server never sees plaintext. Recipients must include the pushing device's own pubkey."
    },
    {
      "name": "env_sync.secret_pull",
      "description": "Pull an encrypted secret blob. Response is ciphertext_b64 only — server never sees and never returns plaintext."
    },
    {
      "name": "env_sync.secret_rotate",
      "description": "Rotate a secret: soft-delete the old node, create a new live node with the same alias and the new ciphertext+recipients. 7-day grace before hard delete of old ciphertext bytes."
    },
    {
      "name": "env_sync_txn_push",
      "description": "Ingest a batched envelope of client-recorded env_sync mutations. HLC-skew gated, partial-success on conflict."
    },
    {
      "name": "env_sync_txn_rollback",
      "description": "Inverse-apply a prior env_sync transaction. Safety gates: ownership, idempotency, descendant-conflict (force opt-out)."
    },
    {
      "name": "tasks_read",
      "description": "Read tasks and task categories. action: tasks | categories (consolidated surface — same handlers as the legacy tools)"
    },
    {
      "name": "task_write",
      "description": "Create or update a task (non-destructive). action: create | update. Deletion is a separate tool (delete_task). (consolidated surface — same handlers as the legacy tools) Required per action — create: content | update: id."
    },
    {
      "name": "dev_docs_read",
      "description": "Read dev docs. action: get | list | categories | pull (consolidated surface — same handlers as the legacy tools)"
    },
    {
      "name": "dev_doc_write",
      "description": "Create or update a dev doc (non-destructive). action: create | update. Deletion is a separate tool (delete_dev_doc). (consolidated surface — same handlers as the legacy tools) Required per action — create: title, content | update: content."
    },
    {
      "name": "sync_read",
      "description": "Read-only vault sync operations. action: list | pull | diff | merge | conflicts. Writes (push/delete) are separate tools — they carry CAS semantics. (consolidated surface — same handlers as the legacy tools) Required per action — diff: path | merge: path, base_text, local_text."
    },
    {
      "name": "vault_read",
      "description": "Read vault metadata. action: list | clone | status (consolidated surface — same handlers as the legacy tools) Required per action — clone: name."
    },
    {
      "name": "env_sync_read",
      "description": "Read env-sync state. action: devices | drift | status | txn_pull. Secret reads are a separate tool (env_sync.secret_pull). (consolidated surface — same handlers as the legacy tools)"
    },
    {
      "name": "env_sync_write",
      "description": "Non-destructive env-sync writes. action: enroll | request_share | heartbeat. Secret push/rotate are separate tools. (consolidated surface — same handlers as the legacy tools) Required per action — enroll: alias, age_pubkey, enrollment_token | request_share: target_device_id, secret_shares, device_id | heartbeat: device_id, installed_skills_hash, installed_hooks_hash."
    },
    {
      "name": "graph_read",
      "description": "Read knowledge-graph nodes. action: get | list (consolidated surface — same handlers as the legacy tools)"
    },
    {
      "name": "graph_write",
      "description": "Add or update a graph entity (non-destructive). action: add | update. Deletion is a separate tool (graph_soft_delete). (consolidated surface — same handlers as the legacy tools) Required per action — add: node_type, alias, payload, device_id | update: node_id, device_id, field_updates."
    },
    {
      "name": "handoff_read",
      "description": "Read session handoffs. action: get | list (consolidated surface — same handlers as the legacy tools) Required per action — get: project, topic."
    },
    {
      "name": "memory_read",
      "description": "Read agent memory. action: get | search (consolidated surface — same handlers as the legacy tools) Required per action — get: source | search: query."
    },
    {
      "name": "auth_key",
      "description": "Obtain an MCP key. action: login | signup (consolidated surface — same handlers as the legacy tools) Required per action — login: email, password | signup: email, password."
    }
  ],
  "toolsPreviewAt": "2026-08-11T23:21:23.897Z",
  "toolsPreviewStatus": "ok"
}
